name: Dev Pipeline

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write
  id-token: write

jobs:
  call-shared-pipeline:
    uses: bhlabz/cicd-pipeline/.github/workflows/shared-pipeline.yml@main
    permissions:
      id-token: write
      contents: write
    with:
      environment: dev
      architecture: 'arm64'
